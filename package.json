{
  "name": "evaluator",
  "version": "1.0.0",
  "description": "Library to parse and evaluate arithmetic expression",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepublish": "npm run build",
    "build": "tsc",
    "test": "jest --verbose",
    "test-watch": "jest --verbose --watchAll"
  },
  "author": "Chendjou deGrace",
  "license": "ISC",
  "dependencies": {
    "@types/jest": "^24.0.11",
    "jest": "^24.7.1",
    "ts-jest": "^24.0.2"
  },
  "devDependencies": {
    "typescript": "^3.4.3"
  }
}
